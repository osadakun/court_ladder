/**
 * FR-05 自動配置テスト
 * v2.6 で自動配置フローを廃止したため、このテストファイルは空。
 * allocateEntries は廃止されたため、テスト対象なし。
 */

Deno.test("FR-05 v2.6 自動配置フロー廃止 → テストなし", () => {
  // v2.6 では初期自動配置を廃止。エントリー作成時に initial_court_no を必須指定する。
});
